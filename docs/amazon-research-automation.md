# Amazon仕入れ候補リサーチ実装・検証記録

対象: `apps-script/amazon-order-importer`

仕様原本: [Codex実装指示書_改訂版.pdf](./Codex実装指示書_改訂版.pdf) および2026-07-01改訂版②の33〜34節

## 16節以降の実装対応

| 要件 | 実装 |
| --- | --- |
| D列の検索ワードを使用 | D列を改行分割し、各ワードを個別検索 |
| C列の売上金以内 | 価格と取得済み送料の合計で判定 |
| 送料不明 | 商品価格がC列以内なら仮候補とし、結果へ `送料要確認` を付記 |
| ジャンク除外 | タイトル・状態・周辺テキストを禁止語で検査 |
| 既存URLを削除しない | 結果セルは追記のみ。削除処理を実装しない |
| URL重複防止 | 商品URLを正規化してセル内URLと比較 |
| 良条件候補のみ追記 | 未登録URLのうち、既存候補より安い、または状態が良い候補だけを追記 |
| muza新品限定 | SKUに `muza` を含む行は新品状態のみ許可 |
| 表示中は継続 | リサーチ管理表のE列状態を終了条件にせず、表示行を毎時再処理 |
| Amazon | 新品および中古「可」まで |
| ヤフオク・メルカリ | 「傷や汚れあり」まで |
| ジモティ | ジャンク品・販売終了でなければ、状態表記が取得できなくても候補化 |
| 楽天市場 | リサーチ管理表J列へ楽天市場候補を追記。既存の`その他サイト`列は互換列として認識 |
| DVD・Blu-ray | 再生不可・欠品・巻数不足を除外 |
| 取得制限 | 処理を止めず、確認用シートへ検索URLを記録 |
| 表示開始日 | 自動処理では表示状態を変えず、専用メニューの明示実行時だけ2026/06/30より前を非表示 |
| 手動編集保護 | 手入力・背景色・手動非表示行・削除列を変更／復元しない |
| 注文番号連動 | 注文番号を唯一のキーとしてメイン表とリサーチ管理表を同期 |
| 削除連動 | メイン表から消えた注文番号の管理表行だけを削除 |
| 削除済み注文の再表示防止 | 削除済み注文番号を`削除済み注文`へ記録し、Gmail再検索時も再追加しない |
| Gmail注文確定取得 | 件名だけでなく本文内の`注文確定`と注文番号も確認し、処理済みラベル付きの直近メールも重複・削除済みチェック後に再評価 |
| 自動取得直後リサーチ | Gmail取り込みで新規追加した注文番号だけを管理表へ即時同期し、その注文だけ同じ実行内でリサーチ開始 |
| 管理表への結果追記 | 条件が改善する候補URL・状態・確認情報をリサーチ管理表へ重複なく反映 |
| 多重実行防止 | `LockService`で1時間処理と同期処理を排他制御 |
| 管理表の例外 | 非表示・不存在・注文番号重複を確認用へ記録し、メイン処理は継続 |

## 継続実行方式

`researchListedItemsHourly()` は実行冒頭でメイン表からリサーチ管理表へ注文番号同期を行い、その後はリサーチ管理表で非表示になっていない行を収集します。`候補あり`や`候補なし`を含め、リサーチ状況は対象除外に使いません。列位置は固定せず、既存ヘッダーから判定します。

Apps Scriptの実行時間上限に備え、処理時間または行数の上限へ到達した場合はスクリプトプロパティにカーソルを保存します。次の1時間トリガーはカーソル位置から再開するため、先頭行だけが繰り返し処理されることを防ぎます。

各実行の冒頭で`syncResearchManagementByOrderNumber()`相当の同期を行います。メイン表から注文自体が削除された場合のみ管理表の対応行を削除し、既存URLやリサーチ結果だけを理由に削除することはありません。

Gmail自動取得で新規注文行が追加された場合は、追加した注文番号リストを使って`リサーチ管理表`へ即時同期し、その注文だけを対象にリサーチを開始します。これにより、注文確定商品リサーチ表への自動取得から管理表側のリサーチ開始までが同一実行内で連動します。既存の表示行全体は再処理しないため、手入力・既存URL・手動非表示行は従来どおり保護されます。

リサーチ管理表は自動リサーチの実行対象シートであり、削除しません。`削除済み注文`はユーザーが削除した注文番号の再表示を防ぐために使用するため、こちらも削除しません。

候補URLの追記は、単にURLが新しいだけでは行いません。既存候補と比較して、合計価格がより安い、または状態ランクがより良い場合だけ追記します。価格・状態のどちらも改善しない候補は確認用にも追加せず、既存の良い候補を優先します。

注文メール取り込み前と行変更トリガー実行時には、注文番号スナップショットとの差分を確認します。スナップショットに存在して現在のメイン表に存在しない注文番号は`削除済み注文`へ記録し、以後のGmail取り込みでは再表示しません。132行目以降をまとめて削除する専用メニューも同じ記録処理を行います。

さらに、セットアップ・Gmail取り込み・行変更トリガーの入口で132行目以降に残っている注文行を自動的に`削除済み注文`へ登録して削除します。これにより、ユーザーがメニューを実行できない場合でも過去に削除した領域を再表示しません。

## 検証

`tests/amazon-order-importer-research.test.js` で以下を自動検証します。

- 価格と送料の上限判定
- 送料不明表示
- ジャンク・動作未確認除外
- サイト別の状態条件
- muza中古除外／新品許可
- DVD全巻判定
- 型番一致
- URL正規化と重複防止
- 既存候補より安い/状態が良い候補だけの追記
- 表示行のみの選択
- カーソル循環
- 既存URLおよび手動入力の保持
- 注文番号抽出と管理表の追加・削除同期

## 2026-07-02 追記: 管理表限定追記と旧シート削除

- 候補URLの追記は `リサーチ管理表` 側だけに行います。`注文確定商品リサーチ表` 側のF〜J列へは追記しません。
- `リサーチ管理シート` は旧名称の未使用シートとして扱い、セットアップ時に存在すれば削除します。
- `リサーチ管理表` は自動リサーチの対象、`削除済み注文` は削除済み注文の再表示防止に必要なため、どちらも削除しません。
- Gmail自動取得直後の管理表追加と対象注文のみの即時リサーチ
- 削除済み注文番号の記録とGmail再追加防止
- リサーチ管理表表示行の毎時リサーチ
- リサーチ管理表J列への楽天市場候補追記
- 管理表側のURL重複防止
